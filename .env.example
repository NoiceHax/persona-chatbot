
# Comma-separated browser origins allowed to call the API via CORS.
# Frontend is same-origin in prod; defaults to https://persona.noicehax.dev when unset.
ALLOWED_ORIGINS=https://persona.noicehax.dev
